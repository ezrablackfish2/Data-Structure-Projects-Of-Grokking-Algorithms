# Smallest Number in Infinite Set

## Problem
Given a binary tree encoded as nested tuples (value, left, right), return
the maximum depth of the tree. Use None for missing children.

Input: tree tuple or None.
Output: integer depth.

Example: (1, (2, None, None), (3, None, None)) -> 2

## Approach
Use a direct, readable implementation based on the problem definition.
