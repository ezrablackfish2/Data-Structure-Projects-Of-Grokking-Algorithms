# First Missing Positive

## Problem
Given a grid with r rows and c columns, return the number of unique paths
from the top-left to the bottom-right moving only down or right.

Input: integers r, c.
Output: integer count.

Example: r=2, c=3 -> 3

## Approach
Use a direct, readable implementation based on the problem definition.
